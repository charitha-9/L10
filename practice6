#include<stdio.h>
int main ()
{ 
  int a,b,c;
  printf("enter the num:");
  scanf("%d",&a,b,c);
  if (a>0)
  {
     printf("positive");
   }
   else if (b>0)
   {
     printf("negative");
   }
   else
   {
     printf("zero");
    }
     return 0;
}
