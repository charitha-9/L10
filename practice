#include<stdio.h>
int main ()
{
  int a;
  printf("enter a num:");
  scanf("%d",&a);
  if (a%2==0)
  {
    printf("it is an even number");
   }else{
    printf("it is an odd number");
    }
     return 0;
}
checkout this 

